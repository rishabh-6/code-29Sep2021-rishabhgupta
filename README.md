* Car Dealership

* Run Server
`cd server/`
`node server.js`

* Run Client
`cd client/`

`yarn start`

* Build Client
`cd client/`

`yarn build`

* Client
`http://localhost:3000/`

* Server
`http://localhost:4000/graphql`



